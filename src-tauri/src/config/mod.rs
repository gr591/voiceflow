// WS1 / shared — thin wrapper around tauri-plugin-store.
// The store is accessed directly by the plugin from TypeScript;
// this module exists for any Rust-side config reads needed by hotkey.rs, tray.rs, etc.
